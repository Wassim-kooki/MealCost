{
  "name": "mealcost",
  "version": "1.0.0",
  "description": "Meal Cost",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
	"package": "electron-packager . mealcost --overwrite --platform=win32 --arch=x64 --electron-version=22.0.0 --icon=icon.ico"
  },
  "author": "Wassim",
  "license": "MIT",
  "devDependencies": {
    "electron": "^22.0.0"
  }
}
